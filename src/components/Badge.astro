---
import { Icon } from "astro-icon";

export interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="inline-flex items-center gap-2">
  <Icon
    class="inline h-6 w-6 stroke-slate-600 stroke-2"
    pack="tabler"
    name={name}
  />

  <slot />
</span>
